import {  } from 'src/actions';

const posts = (store) => (next) => (action) => {
  switch (action.type) {
    case :
      
      next(action);
      break;
    default:
      next(action);
  }
};

export default posts;
